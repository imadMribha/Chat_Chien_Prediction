# Cat vs Dog Simulation

This project simulates supervised learning using image classification.

The system uses RGB averages from user-provided datasets to classify new images and output percentage-based predictions.